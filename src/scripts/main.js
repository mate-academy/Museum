'use strict';

const navToogler = document.querySelector('.icon--menu');
const navWrapper = document.querySelector('.menu');

navToogler.addEventListener('click', () => {
  navWrapper.classList.toggle('menu--active');
});

navToogler.addEventListener();
